<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>

        <record id="ir_cron_hr_attendance_send_mail_action" model="ir.cron">
            <field name="name">Send Checkout Mail</field>
            <field name="model_id" ref="model_hr_attendance"/>
            <field name="state">code</field>
            <field name="code">model.send_checkout_email()</field>
            <field name="interval_number">1</field>
            <field name="interval_type">days</field>
            <field name="numbercall">-1</field>
<!--            <field name="nextcall" eval="(DateTime.now() + timedelta(hours=23)).strftime('%Y-%m-%d %H:%M:%S')"/>-->
        </record>
    </data>
</odoo>